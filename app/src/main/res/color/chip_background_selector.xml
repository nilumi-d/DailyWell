<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Selected state -->
    <item android:color="@color/primary_green_light" android:state_checked="true" />
    <!-- Pressed state -->
    <item android:color="@color/accent_blue_light" android:state_pressed="true" />
    <!-- Default state -->
    <item android:color="@color/card_background" />
</selector>